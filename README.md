# password-tool
A professional-grade Password Strength Analyzer tool evaluates password security using entropy-based analysis (via the zxcvbn library) to detect common patterns, dictionary words, and sequences. It provides real-time feedback, security warnings, and generates cryptographically secure alternatives using Python’s secrets module.
